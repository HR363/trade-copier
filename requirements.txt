# MT5 Trade Copier — Python dependencies
# Install with:  pip install -r requirements.txt

# MetaTrader5 Python integration (Windows only)
MetaTrader5>=5.0.45

# Optional: colored output on older Windows terminals (already built-in on Win10+)
colorama>=0.4.6
